# ML Frameworks
xgboost>=2.0.0
lightgbm>=4.0.0
scikit-learn>=1.3.0
torch>=2.0.0

# Core data + API (optional for CSV-only CLI; required for live/historical feeds)
httpx>=0.27.0
python-dotenv>=1.0.0
mcp>=1.2.0
openai>=1.0.0

# Local API for React UI (scripts/api_server.py)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0

# Cache (parquet preferred; csv fallback works without pyarrow)
pyarrow>=17.0.0

# S&P 500 CSV download (scripts/download_sp500.py)
yfinance>=0.2.40

# Tests
pytest>=8.0.0
